# ☁️ Nextcloud Self-hosted (Docker + Cloudflare Tunnel)

Self-hosted Nextcloud setup using Docker, optimized for Cloudflare Tunnel (no port forwarding).

## 🚀 Features

- Docker Compose
- MariaDB backend
- Cloudflare Tunnel ready
- Mobile auto photo backup

## 🛠 Setup

```bash
cp .env.example .env
docker compose up -d
```

